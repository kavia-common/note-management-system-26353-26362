# Example environment variables for notes_backend
# Copy to .env and set correct values (DO NOT COMMIT SECRETS)

# Database connection (use either full SQLAlchemy URL or individual parts)
# Full SQLAlchemy URL (preferred when available)
# POSTGRES_URL=postgresql+psycopg://user:password@host:5432/dbname

# Or provide individual parts (the app will build the URL):
POSTGRES_URL=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=notes
POSTGRES_PORT=5432

# CORS allowed origins (comma-separated). Use * for all.
ALLOW_ORIGINS=*

# Authentication secret (if applicable for your deployment)
# For demo token-based login, tokens are 'token:<email>'
# AUTH_SECRET can be used in the future to sign JWTs.
AUTH_SECRET=change_me
