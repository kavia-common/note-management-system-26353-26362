# Example environment configuration for Notes Backend

# HTTP server
PORT=3001
HOST=0.0.0.0
ALLOW_ORIGINS=*

# Database configuration
# Option A: Provide a full SQLAlchemy URL (preferred driver is psycopg v3)
# POSTGRES_URL=postgresql+psycopg://appuser:dbuser123@notes_database:5000/myapp

# Option B: Provide individual settings (the app has sane defaults for the multi-container setup)
POSTGRES_USER=appuser
POSTGRES_PASSWORD=dbuser123
POSTGRES_DB=myapp
POSTGRES_PORT=5000
# In multi-container environments DO NOT use localhost for host. Use the service name:
POSTGRES_HOST=notes_database

# Auth config (demo)
# AUTH_SECRET=<set if needed>
